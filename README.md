# sweetpad-lib

Test corpus of resolved Xcode build settings, build plans, and raw inputs captured from real open-source iOS/macOS projects across multiple Xcode versions. Acts as the snapshot oracle that a future Rust resolver must match. See [PLAN.md](PLAN.md) for the full methodology, locked decisions, and step-by-step execution order.
